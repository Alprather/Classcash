<h1>Classes</h1>

<div class="row">
  <div class="col-md-8">
    <% @sections.each do |section| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to section.name, section %>

          </h4>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-md-4">
    <%= link_to "Add New Class", new_section_path, class: 'btn btn-success' %>
  </div>
  </div>
